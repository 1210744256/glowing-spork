<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.baizhi.dao.UserInFoDao">
<!--      T selectById(V id);
    T selectAll();
    void update(T t);
    void deleteById(V id);
    void insert(T t);-->
    <select id="selectById" parameterType="int" resultType="UserInFo">
        select user_id userId,city,introduce,fans,followee,gender,birthday,
               credits,level,create_time createTime,update_time updateTime
        from tb_user_info where user_id=#{id}
    </select>
</mapper>