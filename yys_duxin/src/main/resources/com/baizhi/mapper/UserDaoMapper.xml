<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.baizhi.dao.UserDao">

    <insert id="insert" parameterType="User" useGeneratedKeys="true" keyProperty="id">
        insert into tb_user( phone,
                             password,
                             nick_name,icon,create_time,update_time)
        values (#{phone},#{password},#{nickName},#{icon},#{createTime},#{updateTime})
    </insert>
    <select id="queryByPhone" resultType="com.baizhi.entity.User" parameterType="String">
        select id,
               phone,
               password,
               nick_name   nickName,
               icon,
               create_time createTime,
               update_time updateTime
        from tb_user
        where phone = #{phone}
    </select>
    <select id="queryByIds" resultType="com.baizhi.entity.User">
        select id,nick_name  nickName,icon from tb_user where id in
            <foreach collection="ids" item="id" separator="," open="(" close=")">
                #{id}
            </foreach>

    </select>
</mapper>